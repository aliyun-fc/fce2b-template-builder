[project]
name = "fce2b-template-builder"
version = "0.1.0"
description = "从私有 GHCR 镜像构建 fce2b Template 并执行 Sandbox run_code 验收"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "e2b==2.30.0",
  "e2b-code-interpreter==2.6.0",
]
